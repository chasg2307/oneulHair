# 절대 금지 패턴 (oneulHair)

아래는 현재 코드에서 **실제로 발견된 위험 패턴**을 기준으로 만든 금지 목록입니다.

## 조치 현황 (2026-02-18)
- 완료: 실제 설정 파일 `aa.conf`를 저장소 바깥(`../aa.conf`)으로 이동하고, 코드 기본 로딩 경로를 상위 폴더로 변경 (`main.py`).
- 완료: 저장소에는 비밀값 없는 예시 설정만 유지 (`aa.conf.example`).
- 완료: 민감 URL/응답 본문 출력 제거 (`main.py`).
- 완료: 인증 예외를 성공으로 처리하던 로직 제거 (`naverLogin.py`).
- 완료: `except Exception: pass` 패턴 제거 및 진단 로그 추가 (`main.py`, `naverLogin.py`).
- 완료: 수동 로그인 입력에 타임아웃 추가 (`naverLogin.py`).
- 완료: 문자열 깨짐(mojibake) 상태였던 `naverLogin.py` 메시지/주석 정리.
- 완료: 캐시 산출물 무시 규칙 추가 및 캐시 폴더 정리 (`.gitignore`, `__pycache__/` 제거).

## 1) 비밀정보(계정/비밀번호/키 경로) 하드코딩 및 저장소 포함 금지
- 발견 위치:
  - `aa.conf:36` (`naver_id = ...`)
  - `aa.conf:37` (`naver_pw = ...`)
  - `aa.conf:19` (`service_account_file = ../oneulHair.json`)
- 금지 이유: 계정 탈취, 키 유출, 외부 공유 시 즉시 사고로 이어짐.
- 절대 금지:
  - 설정 파일에 실계정 ID/PW 평문 저장
  - 서비스 계정 키 파일 경로/파일을 저장소에 포함

## 2) 민감 데이터가 포함될 수 있는 URL/응답 본문 로그 출력 금지
- 발견 위치:
  - `main.py:721` (`print(f"REST 호출 시작: {cfg['rest_url']}")`)
  - `main.py:746` (`print(f"REST 응답 본문(일부): {body_preview}")`)
  - `main.py:769` (`print(f"REST 응답 본문(일부): {body_preview}")`)
- 금지 이유: URL 쿼리/본문에 고객명, 연락처, 예약정보가 포함될 수 있어 로그 유출 위험이 큼.
- 절대 금지:
  - 원본 `rest_url` 전체 출력
  - 실패 시 응답 본문 미리보기 출력

## 3) 인증/보안 판정 로직에서 예외를 성공으로 처리 금지
- 발견 위치:
  - `naverLogin.py:46-48` (`except Exception: return True`)
- 금지 이유: 네트워크 오류/차단 상황을 "정상 인증"으로 오판해 비정상 데이터 수집 또는 오동작 유발.
- 절대 금지:
  - 인증 확인 함수에서 예외 발생 시 성공(`True`) 반환

## 4) 광범위 예외 삼키기(`except Exception: pass`) 금지
- 발견 위치:
  - `main.py:259-260`
  - `main.py:264-265`
  - `main.py:705-706`
  - `main.py:740-741`
  - `main.py:764-765`
  - `naverLogin.py:64-65`
- 금지 이유: 장애 원인 추적 불가, 데이터 누락/불일치를 숨김.
- 절대 금지:
  - 로그/재시도/명시적 fallback 없이 예외 무시

## 5) 텍스트 인코딩 깨짐 상태(mojibake) 커밋 금지
- 발견 위치:
  - `naverLogin.py` 문자열/주석 일부가 깨진 상태로 존재했음
  - `aa.conf` 주석/값 일부가 깨져 보이는 상태였음
- 금지 이유: 운영 메시지/설정 오해로 장애 대응이 어려워지고, 유지보수 정확도가 크게 떨어짐.
- 절대 금지:
  - 깨진 문자열 상태 그대로 배포/커밋


## 6) 자동화 코드에서 무기한 대기 입력 사용 금지
- 발견 위치:
  - `naverLogin.py:122` (`input()`)
- 금지 이유: CI/배치 환경에서 프로세스 영구 대기, 장애 전파.
- 절대 금지:
  - 타임아웃 없는 `input()` 기반 제어

## 7) 로컬 실행 산출물(`__pycache__`, `*.pyc`) 버전관리 포함 금지
- 발견 위치:
  - `__pycache__/` 다수 파일 존재
- 금지 이유: 환경 종속 파일로 충돌/노이즈 증가, 재현성 저하.
- 절대 금지:
  - 캐시/바이너리 아티팩트를 저장소에 추가

## 적용 원칙
- 신규 코드 리뷰에서 위 7개 항목 중 하나라도 발견되면 **즉시 차단(merge 금지)**.
- 예외가 필요한 경우에도 "사유 + 보완 통제(마스킹/타임아웃/명시적 에러 처리)" 없이는 허용하지 않음.
